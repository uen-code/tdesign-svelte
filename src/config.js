/**
 * 全局样式前缀
 */
export const classPrefix = 't'

/**
 * 组件大小
 */
export const sizeEnum = {
  small: 'small',
  medium: 'medium',
  large: 'large',
}

/**
 * 组件主题
 */
export const themeEnum = {
  default: 'default',
  primary: 'primary',
  danger: 'danger',
  warning: 'warning',
  success: 'success',
}

