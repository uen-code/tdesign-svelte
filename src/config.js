/**
 * 全局样式前缀
 */
export const classPrefix = 't'

/**
 * 组件大小
 */
export const SIZE_ENUM = {
  small: 'small',
  medium: 'medium',
  large: 'large',
}

/**
 * 组件主题
 */
export const THEME_ENUM = {
  default: 'default',
  primary: 'primary',
  danger: 'danger',
  warning: 'warning',
  success: 'success',
}

